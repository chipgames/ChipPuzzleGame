# 모바일에서 바로 접근하기

## 빠른 가이드

### 📱 Android (안드로이드)

**방법 1: 홈 화면에 추가 (권장)**
1. Chrome 브라우저에서 https://chipgames.github.io/ChipPuzzleGame/ 접속
2. 브라우저 메뉴 (⋮) 클릭
3. **"홈 화면에 추가"** 또는 **"앱 설치"** 선택
4. 확인 버튼 클릭
5. 홈 화면에 앱 아이콘이 생성됨 → 탭하면 바로 실행!

**방법 2: 브라우저 북마크**
1. Chrome에서 사이트 접속
2. 메뉴 (⋮) → **"북마크"** 선택
3. 북마크바에서 바로 접근 가능

**방법 3: 바로가기 생성**
1. Chrome에서 사이트 접속
2. 주소창 옆 **별표 아이콘** 클릭
3. 북마크 저장 후 홈 화면에 추가

### 🍎 iOS (아이폰)

**방법 1: 홈 화면에 추가 (권장)**
1. **Safari 브라우저**에서 https://chipgames.github.io/ChipPuzzleGame/ 접속
   ⚠️ **중요**: Safari만 지원 (Chrome 등 다른 브라우저 불가)
2. 화면 하단 중앙의 **공유 버튼 (□↑)** 클릭
3. 스크롤하여 **"홈 화면에 추가"** 선택
4. 앱 이름 확인 후 **"추가"** 버튼 클릭
5. 홈 화면에 앱 아이콘이 생성됨 → 탭하면 바로 실행!

**방법 2: Safari 북마크**
1. Safari에서 사이트 접속
2. 공유 버튼 (□↑) → **"북마크 추가"** 선택
3. 북마크에서 바로 접근 가능

## 설치 후 사용법

### ✅ 설치 완료 후
- 홈 화면의 앱 아이콘을 탭하면 **바로 게임 실행**
- 브라우저 주소창 없이 **앱처럼 사용** 가능
- 전체 화면 모드로 실행
- 오프라인에서도 일부 기능 사용 가능 (Service Worker 캐싱)

### 📋 주요 기능
- ✅ 빠른 실행 (홈 화면에서 바로 접근)
- ✅ 앱처럼 사용 (브라우저 UI 없음)
- ✅ 오프라인 지원 (일부 기능)
- ✅ 자동 업데이트

## 문제 해결

### 설치 버튼이 보이지 않는 경우

**Android:**
- Chrome 브라우저 최신 버전 사용 확인
- HTTPS 연결 확인 (필수)
- 브라우저 메뉴에서 "홈 화면에 추가" 수동 선택

**iOS:**
- **Safari 브라우저 사용 필수** (Chrome 등 다른 브라우저에서는 불가)
- 공유 버튼에서 "홈 화면에 추가" 옵션 확인
- iOS 11.3 이상 필요

### 앱이 실행되지 않는 경우
1. 브라우저 캐시 삭제
2. 앱 삭제 후 재설치
3. 브라우저 업데이트

## 참고사항

- **PWA (Progressive Web App)**로 설정되어 있어 앱처럼 사용 가능
- 앱 스토어 설치 불필요 (웹 앱)
- 네트워크 연결 권장 (일부 기능은 오프라인 지원)
- 업데이트는 자동으로 적용됨
