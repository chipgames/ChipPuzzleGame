# 모바일에서 홈 화면에 추가하기 (PWA 설치 가이드)

## 개요

이 게임은 PWA(Progressive Web App)로 설정되어 있어 모바일 기기에서 앱처럼 설치하여 사용할 수 있습니다.

## 설치 방법

### Android (Chrome, Samsung Internet 등)

1. **Chrome 브라우저에서 접속**
   - https://chipgames.github.io/ChipPuzzleGame/ 접속

2. **설치 프롬프트 확인**
   - 브라우저 하단에 "홈 화면에 추가" 또는 "설치" 버튼이 표시될 수 있습니다
   - 표시되면 클릭하여 설치

3. **수동 설치 (프롬프트가 없는 경우)**
   - 브라우저 메뉴 (⋮) 클릭
   - "홈 화면에 추가" 또는 "앱 설치" 선택
   - 확인 버튼 클릭

4. **설치 완료**
   - 홈 화면에 "매칭 퍼즐" 아이콘이 생성됩니다
   - 아이콘을 탭하면 앱처럼 실행됩니다

### iOS (Safari)

1. **Safari 브라우저에서 접속**
   - https://chipgames.github.io/ChipPuzzleGame/ 접속

2. **공유 버튼 클릭**
   - 화면 하단 중앙의 공유 버튼 (□↑) 클릭

3. **홈 화면에 추가**
   - 스크롤하여 "홈 화면에 추가" 선택
   - 또는 "홈 화면에 추가" 아이콘 찾기

4. **이름 확인 및 추가**
   - 앱 이름 확인 (기본값: "매칭 퍼즐 게임")
   - "추가" 버튼 클릭

5. **설치 완료**
   - 홈 화면에 앱 아이콘이 생성됩니다
   - 아이콘을 탭하면 Safari 없이 앱처럼 실행됩니다

## 설치 후 사용법

### Android
- 홈 화면의 앱 아이콘을 탭하면 바로 게임이 실행됩니다
- 브라우저 주소창 없이 앱처럼 사용할 수 있습니다
- 오프라인에서도 일부 기능을 사용할 수 있습니다 (Service Worker 캐싱)

### iOS
- 홈 화면의 앱 아이콘을 탭하면 Safari 없이 앱처럼 실행됩니다
- 전체 화면 모드로 실행됩니다
- 브라우저 UI 없이 깔끔하게 게임을 즐길 수 있습니다

## PWA 기능

### 지원 기능
- ✅ 홈 화면에 추가
- ✅ 오프라인 지원 (Service Worker)
- ✅ 빠른 로딩 (캐싱)
- ✅ 전체 화면 모드
- ✅ 앱 아이콘
- ✅ 스플래시 스크린

### 제한 사항
- iOS Safari는 Service Worker 캐싱이 제한적일 수 있습니다
- 일부 브라우저에서는 설치 프롬프트가 자동으로 표시되지 않을 수 있습니다

## 문제 해결

### 설치 버튼이 보이지 않는 경우

**Android:**
1. Chrome 브라우저 최신 버전 사용 확인
2. HTTPS 연결 확인 (필수)
3. 브라우저 메뉴에서 "홈 화면에 추가" 수동 선택

**iOS:**
1. Safari 브라우저 사용 (Chrome 등 다른 브라우저에서는 불가)
2. 공유 버튼에서 "홈 화면에 추가" 옵션 확인
3. iOS 11.3 이상 필요

### 앱이 제대로 작동하지 않는 경우

1. **캐시 삭제**
   - 브라우저 설정에서 캐시 삭제
   - 앱 삭제 후 재설치

2. **Service Worker 확인**
   - 브라우저 개발자 도구에서 Service Worker 상태 확인
   - Service Worker 등록 해제 후 재등록

3. **브라우저 업데이트**
   - 최신 버전의 브라우저 사용

## 기술적 세부사항

### Manifest.json 설정
- `display: "standalone"`: 브라우저 UI 없이 실행
- `orientation: "any"`: 모든 방향 지원
- `start_url`: 앱 시작 URL
- `theme_color`: 상태 표시줄 색상

### Service Worker
- 오프라인 지원
- 리소스 캐싱
- 빠른 로딩

### iOS Safari 특수 설정
- `apple-mobile-web-app-capable`: 전체 화면 모드
- `apple-mobile-web-app-status-bar-style`: 상태 표시줄 스타일
- `apple-touch-icon`: 홈 화면 아이콘

## 참고사항

- PWA 설치 후에도 업데이트가 자동으로 적용됩니다
- 네트워크 연결이 필요하지만, 일부 리소스는 오프라인에서도 사용 가능합니다
- 앱처럼 보이지만 실제로는 웹 앱이므로 앱 스토어 설치가 필요 없습니다
